# 相机与调色复核 · 2026-09-08

> 历史代码审查快照；当前问题和验证结果见 [KNOWN_ISSUES.md](../testing/KNOWN_ISSUES.md) 与 [RELEASES.md](../testing/RELEASES.md)。

本次由主代理进行代码复核，范围包括相机布局与生命周期、CameraX ViewPort、帧坐标与回调、构图线和推荐、相册加载与分析、调色方案及导出。未进行多代理审查。

## 已修复的发现

| 严重度 | 问题和影响 | 修复 |
|---|---|---|
| P1 | 顶部 `onSizeChanged` 位于状态栏及 padding 后，只测内层高度，取景框侵入顶部工具栏 | 改为测量完整外层，初始保留状态栏 + 工具栏高度；底部加入导航栏安全区 |
| P1 | 隐藏工具栏时预留高度降至 24dp，画幅尺寸变化并重新绑定相机 | 保留已经测得的上下区域；显示开关只影响 UI 可见性 |
| P1 | 相机初始化异常未处理，绑定失败只记日志，快门仍可点击 | 提供失败提示、重试与参考照片入口；失败时清除 ImageCapture，禁用快门；拍摄中不切换构图与分析模式 |
| P1 | 帧检测框按全图归一化，忽略 CameraX ViewPort cropRect，方形和黄金比例下框与提示偏移 | 将 cropRect 按 0/90/180/270 度旋转，再按可见裁剪区归一化，排除画外主体 |
| P1 | 关闭智能模式或离开页面后，正在处理的旧帧仍更新结果 | 使用启用状态与代次过滤，清除已排队回调；异常帧立即关闭；未使用检测器时不在关闭阶段创建它 |
| P1 | RectF 的宽和高同时为负时乘积为正，分离的物体被误认为重叠 | 相交尺寸限制为非负，并拒绝退化矩形 |
| P2 | 黄金螺旋对齐点固定为近似横向坐标，竖屏指示位置不匹配曲线 | 对齐到实际六段弧的终点，计入横竖屏、留白及四种镜像 |
| P2 | 方向提示未区分移动手机和主体，可能让用户反向操作 | 文案明确为“让主体在画面中…” |
| P2 | 静态构图分析在 UI 协程进行像素计算，重复检测边缘且吞掉取消 | 移至 Default 调度器，复用边缘结果，传播取消；静态图使用 SINGLE_IMAGE_MODE 并开启多对象检测 |

PreviewView 同时改为 COMPATIBLE（TextureView）模式，以配合 Compose 的定位和裁剪，避免 SurfaceView 在部分设备上的叠层差异。该变化需要设备性能验收。

## 调色复核结论

- 采样参数从 1 开始，避开先前的除零故障；预览与原尺寸导出共用方案。
- 预览使用请求时的方案与强度快照，不将旧结果标为新参数结果。
- AI 参数有范围、曲线、色域及 RGB 模式校验；失败不覆盖编辑。
- RGB 互补模式通过减少另外两个通道执行，不伪称与增加原色亮度等价。
- 保存取消会清理未完成的相册条目；密钥不写入磁盘或 APK。

## 仍需改进与验收

1. 当前实时推荐是主体位置启发式，分数不是训练校准过的审美评分。静态分析仍有固定色彩和谐度等近似值，不应描述为专业 AI 审美评估。
2. 绑定成功不等于第一帧已显示。需要真机继续检查首帧延迟、设备相机服务异常、后台恢复、拍摄和裁剪一致性。
3. 应补充水平仪、人物头顶留白/视线空间、画面边缘干扰提示和候选裁剪框，优先提高指引质量。
4. 大尺寸照片导出内存、API 24–25 色彩管理、不同传感器/屏幕比例仍需真机覆盖。

回归测试覆盖上下区域避让、横竖屏尺寸、四向旋转、裁剪框映射、分离矩形、螺旋对齐点，并保留所有调色回归。此报告是代码审查和自动化结果，不替代真机截图验收。

## 本次验证结果

Debug APK 构建成功；37 项 JVM 测试全部通过（0 失败），包含新增的 6 项几何回归及全部调色回归；lint 完成，0 错误。没有完成本次修复的真机取景截图与成片对照验收，不能据此宣称所有机型已验证。
